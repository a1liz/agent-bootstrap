# {{PROJECT_NAME}}

## Purpose

Describe the project objective here.

## Repository Layout

- `src/`: source code
- `tests/`: tests
- `docs/`: operating and adoption notes
- `artifacts/runs/`: run-local runtime outputs
- `schemas/`: state and event schema examples

## Runtime Workspace

Runtime state, logs, generated files, and evaluation outputs should go under `artifacts/runs/<run-name>/`.

## Common Commands

Document the most common run, test, and validation commands for this project here.

## Logs And State

Operators should be able to find logs and saved state under the runtime workspace. This project was bootstrapped from `{{BOOTSTRAP_SOURCE}}` at `{{BOOTSTRAP_VERSION}}`.
