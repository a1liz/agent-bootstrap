# Evals

## Baseline Expectation

Define at least one repeatable evaluation path that is cheap enough to run often and stable enough to serve as a regression signal.

## Output Location

Evaluation outputs should be written to the active run directory unless they are promoted into durable reports.
